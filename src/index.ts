import { createPlugin } from '@bluebase/core';

export default createPlugin({
	description: 'Provides Grid components to create Responsive Layouts',
	key: '@bluebase/plugin-responsive-grid',
	name: 'BlueBase Responsive Grid',
	version: '1.0.0',
});
